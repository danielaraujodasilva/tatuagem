(()=>{document.addEventListener('click',e=>{const trigger=e.target.closest('[data-human]');if(!trigger)return;e.preventDefault();e.stopImmediatePropagation();const modal=document.querySelector('#flowModal'),box=document.querySelector('#flowContent');box.innerHTML='<div class="flow-inner"><span class="kicker">Atendimento humano</span><h2>Conte como podemos ajudar</h2><p>Um atendente verá seu pedido na fila e falará com você pelo canal escolhido.</p><form id="humanTicketForm" class="flow-form"><div class="field"><label>Seu nome</label><input name="name" required placeholder="Como podemos te chamar?"></div><div class="field"><label>WhatsApp ou telefone</label><input name="phone" required placeholder="(11) 99999-9999"></div><div class="field"><label>Como prefere ser atendido?</label><select name="channel"><option>WhatsApp</option><option>Telefone</option><option>Presencial</option></select></div><div class="field"><label>O que você precisa resolver?</label><textarea name="description" required placeholder="Explique o que aconteceu do seu jeito."></textarea></div><div class="flow-actions"><button type="button" class="btn secondary" data-human-cancel>Cancelar</button><button class="btn primary" type="submit">Enviar para atendimento</button></div></form></div>';modal.showModal();box.querySelector('[data-human-cancel]').onclick=()=>modal.close();box.querySelector('form').onsubmit=async ev=>{ev.preventDefault();const f=new FormData(ev.target),b=ev.target.querySelector('button[type=submit]');b.disabled=true;b.textContent='Enviando…';const r=await fetch('./api.php?action=request',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.get('name'),phone:f.get('phone'),service:'Atendimento humano',description:f.get('description'),channel:f.get('channel')})});const d=await r.json();if(!d.ok){b.disabled=false;b.textContent='Enviar para atendimento';return}box.innerHTML='<div class="flow-inner"><span class="kicker">Pedido recebido</span><h2>Pronto, seu atendimento entrou na fila.</h2><p>Seu código é <strong>'+d.code+'</strong>. A equipe entrará em contato por <strong>'+f.get('channel')+'</strong>.</p><div class="flow-actions"><button class="btn primary" data-human-done>Fechar</button></div></div>';box.querySelector('[data-human-done]').onclick=()=>modal.close()};},true)})();
