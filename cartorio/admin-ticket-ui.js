(()=>{const style=document.createElement('style');style.textContent='.ticket-action{margin-left:auto;background:#0f766e;color:#fff;border:0;border-radius:8px;padding:8px 12px;cursor:pointer;font-weight:700}.ticket-modal{position:fixed;inset:8% 5%;z-index:1000;background:#fff;border:1px solid #d0d5dd;border-radius:18px;padding:24px;box-shadow:0 20px 70px #10182855;overflow:auto}.ticket-modal h2{margin:0 0 6px}.ticket-form{display:grid;grid-template-columns:1fr 1fr;gap:12px}.ticket-form label{display:grid;gap:5px;font-size:12px;font-weight:700}.ticket-form input,.ticket-form select,.ticket-form textarea{padding:10px;border:1px solid #d0d5dd;border-radius:8px;font-size:15px}.ticket-form textarea{min-height:90px}.ticket-form .wide{grid-column:1/-1}.ticket-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:18px}.ticket-close{position:absolute;right:18px;top:14px;border:0;background:none;font-size:24px}@media(max-width:650px){.ticket-form{grid-template-columns:1fr}}';document.head.append(style);const open=async code=>{const d=await(await fetch('./api.php?action=admin_detail&code='+encodeURIComponent(code))).json();if(!d.ok)return;const x=d.request,m=document.createElement('div');m.className='ticket-modal';m.innerHTML=`<button class="ticket-close">×</button><h2>Gerenciar ${x.code}</h2><p>${x.service} · criado em ${x.created_at}</p><form class="ticket-form"><label>Cliente<input name="name" value="${x.name||''}"></label><label>Telefone<input name="phone" value="${x.phone||''}"></label><label>Serviço<input name="service" value="${x.service||''}"></label><label>Status<select name="status">${['novo','coleta','analise','aguardando','protocolado','exigencia','entregue'].map(s=>`<option ${s===x.status?'selected':''}>${s}</option>`).join('')}</select></label><label>Responsável<input name="assigned_to" value="${x.assigned_to||''}"></label><label>Prioridade<select name="priority"><option ${x.priority==='normal'?'selected':''}>normal</option><option ${x.priority==='high'?'selected':''}>high</option></select></label><label>Prazo<input name="deadline" value="${x.deadline||''}"></label><label>Taxa oficial<input name="official_amount" type="number" step="0.01" value="${x.official_amount||0}"></label><label>Serviço CNJP<input name="service_amount" type="number" step="0.01" value="${x.service_amount||0}"></label><label class="wide">Observações<textarea name="notes">${x.notes||x.description||''}</textarea></label><div class="wide"><strong>Histórico</strong><div>${d.events.map(e=>`<p>${e.created_at} — ${e.event}: ${e.detail}</p>`).join('')||'<p>Sem eventos.</p>'}</div></div><div class="ticket-actions wide"><button type="button" class="ticket-cancel">Cancelar</button><button class="btn primary" type="submit">Salvar alterações</button></div></form>`;document.body.append(m);m.querySelector('.ticket-close').onclick=m.querySelector('.ticket-cancel').onclick=()=>m.remove();m.querySelector('form').onsubmit=async e=>{e.preventDefault();const o=Object.fromEntries(new FormData(e.target));o.code=code;o.official_amount=Number(o.official_amount);o.service_amount=Number(o.service_amount);o.amount=o.official_amount+o.service_amount;const r=await fetch('./api.php?action=admin_update',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(o)});if((await r.json()).ok){m.remove();location.reload()}}};const bind=()=>document.querySelectorAll('.case-row,.kanban-card').forEach(row=>{if(row.querySelector('.ticket-action'))return;const code=row.dataset.liveCode||(row.textContent.match(/(?:CD|BETA)-[A-Z0-9-]+/)||[])[0];if(!code)return;const b=document.createElement('button');b.className='ticket-action';b.textContent='Gerenciar ticket';b.onclick=e=>{e.stopPropagation();open(code)};row.append(b);row.querySelector('[data-crud-edit]')?.remove()});bind();setInterval(bind,700)})();
