(()=>{document.addEventListener('click',e=>{const b=e.target.closest('#guidedSuggest');if(!b)return;b.disabled=true;b.dataset.original=b.textContent;b.textContent='Analisando seu caso…';const box=document.querySelector('#guidedSuggestions');if(box){box.hidden=false;box.innerHTML='<p class="ai-loading">A IA local está lendo seu relato e comparando com o catálogo de serviços. Isso pode levar alguns segundos.</p>';new MutationObserver(()=>{if(box.querySelector('.guided-result')){b.disabled=false;b.textContent=b.dataset.original||'Sugerir caminhos'}}).observe(box,{childList:true,subtree:true})}},true)})();
